import { Outlet, Link } from "react-router-dom";

const Home = () => {
  return (
    <>
      <nav>
        <ul>
          <li>
          </li>
        </ul>
      </nav>

      <Outlet />
    </>
  )
};

export default Home;

